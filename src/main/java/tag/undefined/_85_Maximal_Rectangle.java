package tag.undefined;

/**
 * Created by he.dong on 10/30/2017.
 */
public class _85_Maximal_Rectangle {
    /* failed to resolve. Refer to No.84 */
    /**
     * Inspired by Yuxiong, treat the matrix as multiple histograms based on each row. E,g.
     *
     * matrix[0][] is a histogram with height [0,1] in each pillar.
     * matrix[n][] is a histogram with height [0,n] in each pillar.
     * then the problem degrades to problem No.84.
     * */
    public int maximalRectangle(char[][] matrix) {
        return -1;
    }

}
